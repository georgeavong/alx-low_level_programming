#include <stdio.h>

/** main - outputs "with proper grammar, but the outcome is a piece of art," using the printf function
 *
 * return - returns the value of 0
 */
int main()
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);

}
